import {AsyncCourseService} from './async-course-service';

describe('AsyncCourseService', () => {
  it('should create an instance', () => {
    expect(new AsyncCourseService()).toBeTruthy();
  });
});
